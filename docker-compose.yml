# local redis용 docker-compose
services:
  redis:
    image: redis:alpine
    container_name: edison-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_volume:/data
    command: redis-server --appendonly yes
    restart: always

volumes:
  redis_volume: